name: CI

on:
  push:
    branches:
      - '**'
    tags:
      - '*'
  workflow_dispatch:

jobs:
  package:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Extract tag name
        id: tag
        run: echo "TAG_NAME=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
      
      - name: Extract version
        id: version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            TAG_NAME=${GITHUB_REF#refs/tags/}
            VERSION=${TAG_NAME#v}
          else
            VERSION=${GITHUB_SHA:0:7}
          fi
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
      
      - name: Update version in .toc file
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          # Update the Version line in DeltaChess.toc (use | delimiter so / in refs/heads/... doesn't break sed)
          sed -i "s|^## Version:.*|## Version: $VERSION|" DeltaChess.toc

      - name: Create Package (untagged)
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        uses: BigWigsMods/packager@master

      - name: Clean unused files
        run: |
          ls -la .
          ls -la .release

      - name: Upload package artifact
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        uses: actions/upload-artifact@v4
        with:
          name: DeltaChess-${{ steps.version.outputs.VERSION }}.zip
          path: .release/DeltaChess-${{ steps.version.outputs.VERSION }}.zip

      - name: Create Package (release)
        if: startsWith(github.ref, 'refs/tags/')
        uses: BigWigsMods/packager@master
        env:
          CF_API_KEY: ${{ secrets.CF_API_KEY }}
          GITHUB_OAUTH: ${{ secrets.GITHUB_TOKEN }}
          WOWI_API_TOKEN: ${{ secrets.WOWI_API_TOKEN }}
          WAGO_API_TOKEN: ${{ secrets.WAGO_API_TOKEN }}